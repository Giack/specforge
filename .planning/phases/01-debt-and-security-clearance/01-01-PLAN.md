---
phase: 01-debt-and-security-clearance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/ai/client_test.go
  - internal/jira/client_test.go
  - internal/vcs/github_test.go
  - cmd/dev/wave_test.go
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Every failing bug has a test that will go RED before the fix and GREEN after"
    - "Test files compile and run with `go test ./...` without errors"
    - "FOUND-01 test asserts non-empty response (not just err == nil)"
    - "FOUND-02 test asserts no `bash` appears in the exec.Command chain"
    - "FOUND-03 test asserts Timeout >= 30s on each HTTP client constructor"
  artifacts:
    - path: "internal/ai/client_test.go"
      provides: "Failing tests for FOUND-01 (wrong struct) and FOUND-03 (ai timeout)"
    - path: "internal/jira/client_test.go"
      provides: "Failing tests for FOUND-03 (jira/confluence timeout)"
    - path: "internal/vcs/github_test.go"
      provides: "Failing tests for FOUND-03 (vcs timeout)"
    - path: "cmd/dev/wave_test.go"
      provides: "Failing tests for FOUND-02 (shell injection via bash)"
  key_links:
    - from: "internal/ai/client_test.go"
      to: "internal/ai/client.go"
      via: "httptest server returning Anthropic-shaped JSON"
      pattern: "httptest\\.NewServer"
    - from: "cmd/dev/wave_test.go"
      to: "cmd/dev/wave.go"
      via: "inspect exec.Cmd for bash presence"
      pattern: "exec\\.Command"
---

<objective>
Create failing test scaffolds for all three bugs before any production code changes.

Purpose: Establish the RED state for TDD. Every fix in Wave 2 plans lands against tests that already exist and are already failing. This prevents the silent-success trap where a fix passes because no test was written.
Output: Four test files that compile, run, and fail against the current (buggy) production code.
</objective>

<execution_context>
@/Users/gsortino/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gsortino/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Key signatures the test files must target. Extracted from production source. -->

From internal/ai/client.go:
```go
// Current (buggy) response struct — test must assert this is wrong
type ClaudeResponse struct {
    Choices []Choice `json:"choices"`  // Wrong: should be Content []ContentBlock
}
type Choice struct {
    Message Message `json:"message"`
}

// AIClient struct — no client field (Pitfall 1: timeout not on struct)
type AIClient struct {
    config config.AIConfig
}
func NewAIClient(cfg config.AIConfig) *AIClient

// callClaude — creates &http.Client{} inline at line 147
func (a *AIClient) callClaude(prompt string) (string, error)
```

From internal/jira/client.go (confirmed from research):
```go
// NewJiraClient at line 28 — uses &http.Client{} no timeout
// NewConfluenceClient at line 136 — uses &http.Client{} no timeout
```

From internal/vcs/github.go (confirmed from research):
```go
// NewGitHubClient at line 33 — uses &http.Client{} no timeout
```

From cmd/dev/wave.go:
```go
// Vulnerable functions — use bash wrapper script
func executeWithClaude(prompt, taskName string) error   // lines 76-98
func executeWithOpenCode(prompt, taskName string) error  // lines 100-119
// Both write fmt.Sprintf(script) to /tmp/specforge_exec.sh, run exec.Command("bash", scriptFile)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for FOUND-01 and FOUND-03 (ai client)</name>
  <files>internal/ai/client_test.go</files>
  <behavior>
    - TestCallClaudeResponseDecoding: spin up httptest.Server returning `{"content":[{"type":"text","text":"hello"}]}`, call callClaude via an exported helper or by calling GenerateSpec with provider=claude pointed at the test server — must FAIL because current struct reads `choices[]` and returns empty string; assert `len(response) > 0`
    - TestAIClientHasTimeout: call NewAIClient, then inspect that the http.Client used in callClaude has Timeout >= 30s — must FAIL because &http.Client{} is created inline with no Timeout; use a reflect or a test-server-delay approach: start a handler that sleeps 35s, set client timeout to 1s via test injection, confirm the request fails with a timeout error rather than hanging
    - Note: callClaude is unexported; use a package-level test (package ai) to access it directly
  </behavior>
  <action>
    Create `internal/ai/client_test.go` with `package ai` (white-box test — same package).

    Test 1 (TestCallClaudeResponseDecoding):
    - Start an `httptest.NewServer` that writes `{"content":[{"type":"text","text":"hello world"}]}` with status 200 and Content-Type application/json
    - Construct an AIClient pointing at the test server URL (you will need to either export a field or temporarily add a testable hook — use a package-level var `var claudeAPIURL = "https://api.anthropic.com/v1/messages"` in client.go that the test overrides, OR restructure callClaude to accept a URL; the cleanest approach for a white-box test is to add `var claudeBaseURL = "https://api.anthropic.com/v1/messages"` as a package-level var so the test can set `claudeBaseURL = ts.URL`)
    - Set `ANTHROPIC_API_KEY` env var to a dummy value ("test-key") for the duration of the test
    - Call a.callClaude("test prompt") and assert: err == nil AND len(response) > 0
    - This test MUST FAIL against the current buggy struct (returns empty string because `result.Choices` is nil)

    Test 2 (TestAIClientTimeout):
    - Add a `var httpClientTimeout = 30 * time.Second` package-level var in client.go (test will override to 1s)
    - OR: test that the http.Client{} created in callClaude uses the package-level timeout var
    - Simplest approach: add `var newHTTPClient = func() *http.Client { return &http.Client{Timeout: httpClientTimeout} }` in client.go; test overrides `newHTTPClient` to return a client with 1s timeout, starts a slow server (sleeps 5s), calls callClaude, asserts the error contains "timeout" or "deadline exceeded" within 3s
    - This test MUST FAIL against the current code (no timeout; request would hang)
    - Use `t.Parallel()` on both tests

    Run `go test ./internal/ai/... -run "TestCallClaude|TestAIClient" -v` — expect FAIL (RED state confirmed).
  </action>
  <verify>
    <automated>cd /Users/gsortino/Workspace/gsd-for-enterprise && go build ./internal/ai/... && go test ./internal/ai/... -run "TestCallClaude|TestAIClient" -v -timeout 30s 2>&1 | tail -20</automated>
  </verify>
  <done>Test file compiles. Both tests run and FAIL (exit code 1) against current production code. No compilation errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for FOUND-03 (jira/vcs) and FOUND-02 (shell injection)</name>
  <files>internal/jira/client_test.go, internal/vcs/github_test.go, cmd/dev/wave_test.go</files>
  <behavior>
    - TestJiraClientTimeout: NewJiraClient returns a JiraClient whose http.Client has Timeout >= 30s — FAIL now (no timeout)
    - TestConfluenceClientTimeout: NewConfluenceClient returns a ConfluenceClient whose http.Client has Timeout >= 30s — FAIL now
    - TestGitHubClientTimeout: NewGitHubClient returns a GitHubClient whose http.Client has Timeout >= 30s — FAIL now
    - TestExecuteWithClaudeNoShell: capture the exec.Cmd that executeWithClaude would run; assert the command name is "claude" (not "bash"); assert no argument equals a shell script path; assert stdin pipe is used — FAIL now (runs bash)
    - TestExecuteWithOpenCodeNoShell: same assertion for executeWithOpenCode — FAIL now
  </behavior>
  <action>
    Read `internal/jira/client.go` and `internal/vcs/github.go`, `internal/vcs/gitlab.go`, `internal/vcs/bitbucket.go` first to confirm exact struct field names for the http.Client.

    **internal/jira/client_test.go** (package jira, white-box):
    - TestJiraClientTimeout: call NewJiraClient with a minimal config (empty strings OK for URL/user/token), access the returned struct's `client` field (or however the field is named), assert `client.Timeout >= 30*time.Second`. If the field is unexported, use reflect: `reflect.ValueOf(c).Elem().FieldByName("client")`. This test FAILS now because Timeout is zero.
    - TestConfluenceClientTimeout: same pattern for NewConfluenceClient

    **internal/vcs/github_test.go** (package vcs or package github — match existing file's package declaration):
    - TestGitHubClientTimeout: call NewGitHubClient with a dummy token, access the http.Client field, assert Timeout >= 30s. FAILS now.
    - Also add TestGitLabClientTimeout and TestBitbucketClientTimeout if gitlab.go and bitbucket.go exist in the same package — read those files before writing.

    **cmd/dev/wave_test.go** (package dev, white-box):
    - The challenge: executeWithClaude and executeWithOpenCode run real subprocesses. To make them testable without executing real binaries, add a package-level var `var execCommand = exec.Command` in wave.go. The test overrides `execCommand` to a function that captures the Cmd args without running it.
    - TestExecuteWithClaudeNoShell: override execCommand to capture args; call executeWithClaude("test prompt", "test-task"); assert args[0] == "claude" (not "bash"); assert no argument contains "/tmp/specforge"; assert that the function attempted to write to stdin (capture via the captured Cmd's StdinPipe — this is the RED state; current code doesn't use StdinPipe so the assertion will fail)
    - Simplest RED test: just assert `args[0] != "bash"` — this will FAIL now because current code passes "bash" as the command

    Run `go test ./internal/jira/... ./internal/vcs/... ./cmd/dev/... -v -timeout 30s` — expect FAIL (RED).
  </action>
  <verify>
    <automated>cd /Users/gsortino/Workspace/gsd-for-enterprise && go build ./... && go test ./internal/jira/... ./internal/vcs/... ./cmd/dev/... -v -timeout 30s 2>&1 | tail -30</automated>
  </verify>
  <done>All three test files compile. Tests run and FAIL against current code (RED state). `go build ./...` succeeds — no compilation errors in production code from test hooks added.</done>
</task>

</tasks>

<verification>
After both tasks: `go build ./...` must succeed (no compilation errors). `go test ./...` should report failures (RED) only in the new test functions — no pre-existing tests should newly break.
</verification>

<success_criteria>
- Four test files exist: `internal/ai/client_test.go`, `internal/jira/client_test.go`, `internal/vcs/github_test.go`, `cmd/dev/wave_test.go`
- `go build ./...` exits 0
- `go test ./...` exits non-zero — specifically the new tests fail, not any pre-existing passing tests
- Test names match the verification map in 01-VALIDATION.md: TestCallClaudeResponse, TestHTTPClientTimeout, TestExecuteWithClaudeNoShell
</success_criteria>

<output>
After completion, create `.planning/phases/01-debt-and-security-clearance/01-01-SUMMARY.md`
</output>
